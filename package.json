{
  "name": "yarn-plugin-check-engines",
  "description": "A Yarn plugin to check workspace 'engines' consistency.",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "builder build plugin"
  },
  "devDependencies": {
    "@yarnpkg/builder": "^4.1.1",
    "@yarnpkg/cli": "^4.3.1",
    "@yarnpkg/core": "^4.1.1",
    "semver": "^7.7.2"
  },
  "packageManager": "yarn@4.1.1"
}
